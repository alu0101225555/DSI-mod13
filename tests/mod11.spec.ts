import "mocha";
import {expect} from "chai";
import {cliente} from '../src/mod11/cliente';
import {servidor} from '../src/mod11/servidor';

describe('Test modificación 11', () => {
    const a = new servidor();

    const c = new cliente();

    /*
    it('Servidor: "Error en comando. No existe"', () => {
        a.run('ls', (error) => {
            if(error) {
                expect(error).to.be.equal('ERROR en comando "ls": NO EXISTE');
                done();
            }
        });
    });
    */






})